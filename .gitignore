# MCP / secrets - NUNCA commitear
# Patron .mcp* cubre .mcp, .mcp.json, .mcp.local.json y cualquier variante futura
# (los archivos MCP contienen GITHUB_PERSONAL_ACCESS_TOKEN, API keys, etc.).
.mcp*
# Excepcion: la plantilla SIN secretos si se versiona (referencia para el equipo).
!.mcp.json.example
.env
.env.local
.env.*.local
.env.test
.env.uat
.env.prod
# .env.example SI se commitea (plantilla publica sin secretos)
!.env.example

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Playwright
test-results/
playwright-report/
playwright/.cache/
blob-report/
.playwright/
*.spec.ts.bak

# Visual regression baselines (decision pending)
# tests/**/screenshots/

# Refs (clones read-only de repos V1/V2)
refs/v1/
refs/v2/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# Coverage / reports
coverage/
.nyc_output/
*.log

# Worktrees
.claude/worktrees/

# Claude: settings.json de equipo SE versiona; lo personal/local NO
.claude/settings.local.json

# Tooling interno (perfil, skills, memoria) - no expuesto en el repo publico
CLAUDE.md
memory/
MEMORY.md
npm-install.log
evidence/
storage/
